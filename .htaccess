# ============================
# React / Vite Single Page App Rewrite
# ============================

Options -MultiViews
RewriteEngine On

# If file or directory does not exist, serve index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [QSA,L]
